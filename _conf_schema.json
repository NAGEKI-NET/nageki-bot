{
  "NAGEKI_API_URL": {
    "description": "Nageki-Net API 服务器地址",
    "type": "string",
    "default": "https://nageki-net.com/"
  },
  "NAGEKI_CDN_URL": {
    "description": "Nageki-Net CDN 服务器地址",
    "type": "string",
    "default": "https://cdn-nageki-next.sys-all.com.cn"
  },
  "NAGEKI_TOKEN": {
    "description": "Nageki-Net 登录后的 JWT Token，Rating 查询必填",
    "type": "string",
    "default": ""
  },
  "NAGEKI_PROFILE_RENDER_URL": {
    "description": "前端资料截图渲染页地址，默认使用本机 Angular 开发服务",
    "type": "string",
    "default": "http://localhost:4200/render/ongeki-profile"
  },
  "NAGEKI_RATING_RENDER_URL": {
    "description": "前端 Rating 截图渲染页地址，默认使用本机 Angular 开发服务",
    "type": "string",
    "default": "http://localhost:4200/render/ongeki-rating"
  },
  "NAGEKI_PROFILE_RENDER_THEME": {
    "description": "前端截图主题，可选 light 或 dark",
    "type": "string",
    "default": "dark"
  },
  "NAGEKI_PROFILE_RENDER_LANGUAGE": {
    "description": "前端截图语言，可选 zh、en 或 ja",
    "type": "string",
    "default": "zh"
  },
  "NAGEKI_PLAYWRIGHT_AUTO_INSTALL": {
    "description": "缺少 Playwright Chromium 时是否自动安装",
    "type": "bool",
    "default": true
  },
  "NAGEKI_PLAYWRIGHT_INSTALL_TIMEOUT": {
    "description": "自动安装 Playwright Chromium 的超时时间（秒）",
    "type": "int",
    "default": 600
  },
  "NAGEKI_PLAYWRIGHT_INSTALL_WITH_DEPS": {
    "description": "Linux 环境是否同时安装 Playwright 系统依赖，通常需要更高权限",
    "type": "bool",
    "default": false
  },
  "BOT_API_URL": {
    "description": "QQ 机器人 API 地址，不使用绑定功能可留空",
    "type": "string",
    "default": "http://localhost:8080"
  },
  "BOT_API_KEY": {
    "description": "QQ 机器人 API 密钥，不使用绑定功能可留空",
    "type": "string",
    "default": ""
  }
}
